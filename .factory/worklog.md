# Worklog

Append-only. One entry per task cycle or session, one bullet stamped
`- YYYY-MM-DD HH:MM UTC - ` (date and 24-hour clock time, UTC), then task id,
what happened, decisions and why, verification commands run, follow-ups.
Newest at the bottom.

---

- 2026-08-19 03:21 UTC - Imported 9 open GitHub issues with no label filter as T1-T9, added the rolling parked-minors batch T10, and recorded four issue-defined blockers as Q1-Q4. Set the external issue tracker as the approved spec and moved the new plan to its sole human approval gate. Verification: `gh issue list --state open --limit 1000 --json number,title,body,labels` returned 9 issues; no production tests run (bookkeeping-only import).
- 2026-08-19 03:25 UTC - Approved the populated plan with `spec_approved: true`; set `plan_approved: true` and phase `build` because runnable tasks remain. Verification: confirmed non-empty Approach, Tasks, Risks, and Open questions sections in `.factory/plan.md`; no production tests run (bookkeeping-only approval).
- 2026-08-19 03:37 UTC - T4 shipped as PR #29. Repaired both path-derived SQLite identities so a surrogate-bearing transcript path can commit its fallback session and watermark atomically; regression coverage proves an unchanged second pass begins at the committed offset and does not double-count. Verification: `uv run pytest` (492 passed), `uv run ruff check .` (passed), and `uv run ruff format --check .` (passed). Panel: 1 round; findings 0 blocking / 0 minor / 0 invalid; 0 fixed; shipped standard (planned standard).
- 2026-08-19 03:49 UTC - T4 PR #29 review tally: Codex delivered 2 rounds, with 1 blocking finding fixed in push `8565767` and 1 minor parked in T10 (0 refuted; 1 fix push). Claude Code Review is not configured on this PR head: the legacy workflow is registered remotely but `.github/workflows/claude-code-review.yml` is absent from both the PR and default branch, so it produced no head run. CI: 1 run / 0 reruns, green on `8565767`. The parked-minor thread received its durable disposition reply; no minors-only push was made. Verification: GitHub PR reviews, all review threads, workflow/run history, and CI head check inspected.
- 2026-08-19 03:51 UTC - T4 merged as PR #29 (`ce6b456`). Post-merge bookkeeping marked the task complete, cleared the tracked PR state, and parked the verifier-classified minor in rolling batch T10 and issue #30. The repository ruleset requires resolved threads, so the parked-minor reply linked #30 before resolution. External review: Codex 2 rounds, 1 blocking fixed / 1 minor parked / 0 refuted, 1 fix push; CI 1 run / 0 reruns. Verification: `factory-git merge 29 8565767b33b850425beed86181314429b57fe8f2` completed and recorded PR metrics.
- 2026-08-19 04:06 UTC - T5 shipped as PR #31. The remote usage fetch now runs concurrently with the local attribution scan, allowing the usage sample to land while a slow first scan continues; `poll_once` still waits for both paths and aggregation remains independent of fetch success. Verification: `uv run pytest` (493 passed), `uv run ruff check .` (passed), and `uv run ruff format --check .` (36 files already formatted). Panel: 1 round; findings 0 blocking / 0 minor / 0 invalid; 0 fixed; shipped standard (planned standard).
- 2026-08-19 04:10 UTC - T5 PR #31 review tally: Codex delivered 1 clean round on head `b7170ad` (0 blocking / 0 minor / 0 refuted; 0 fix pushes), verified by its attributable single-head PR-body +1 reaction. Claude Code Review is not configured: `.github/workflows/claude-code-review.yml` is absent at the PR head and no run exists. CI: 1 run / 0 reruns, green on `b7170ad`. No review threads or hold label. Verification: GitHub PR checks, reviews/comments/reactions, workflow history, and review-thread GraphQL query inspected.
- 2026-08-19 04:11 UTC - T5 merged as PR #31 (`3eff8f0`). Post-merge bookkeeping marked the task complete and cleared the tracked PR state. External review: Codex 1 clean round, 0 blocking / 0 minor / 0 refuted, 0 fix pushes; CI 1 run / 0 reruns. Verification: `factory-git merge 31 b7170ad12125bf1035858fefc1a9c44622d962d8` completed and recorded PR metrics.
- 2026-08-19 04:32 UTC - T6 shipped as PR #32. Attribution aggregation now records completion-time freshness without advancing it on failure, exposes health through `/api/attribution`, and renders accessible fresh, failed, stale, pending, and unavailable states while retaining frozen counts. Manually inspected every UI render branch and the served DOM/JS wiring. Verification: `uv run pytest` (497 passed), `uv run ruff check .` (passed), `uv run ruff format --check .` (36 files already formatted), and `node --check src/burnrate/static/app.js` (passed). Panel: 2 rounds; findings 0 blocking / 1 minor / 0 invalid; 1 fixed; shipped standard (planned standard).
- 2026-08-19 04:41 UTC - T6 PR #32: verifier classified Codex's filesystem scan-health finding as blocking. Pushed fix for Codex findings: `3869ba127ce4d5c2504dc1bd29ea7e84b9a76fb3`; it preserves the prior attribution success timestamp and marks the rollup stale when scan or read errors prevent a complete refresh. Replied to and resolved the acted-on thread. Verification: `uv run ruff format --check .`, `uv run ruff check .`, and `uv run pytest` (498 passed). New-head CI and Codex review are pending.
- 2026-08-19 04:51 UTC - T6 PR #32: verifier classified both new Codex findings as blocking. Pushed fix for Codex findings: `64ffbdc91122ea119047a56d7b9b98a246eb262e`; aggregation now uses one health-checked filesystem scan and atomically retains consumed offsets when post-read metadata fails. Replied to and resolved both acted-on threads. Verification: `uv run pytest -q` (499 passed), `uv run ruff format --check .` (36 files already formatted), and `uv run ruff check .` (passed). New-head CI and Codex review are pending.
- 2026-08-19 04:58 UTC - T6 PR #32: verifier classified Codex's unreadable nested-directory scan finding as blocking. Pushed fix for Codex findings: `ee05512ec6e4743a34731398d3c0e5ee13b8a247`; `scan_jsonl_files()` now reports partial `os.walk()` traversal failures without discarding readable paths, so stale rollups cannot be reported as fresh. Replied to and resolved the acted-on thread. Verification: `uv run ruff format --check .`, `uv run ruff check .`, and `uv run pytest -q` (500 passed). New-head CI and Codex review are pending.
- 2026-08-19 04:58 UTC - T6 PR #32 correction: the preceding entry’s expanded fix SHA was transcribed incorrectly; the pushed commit is `ee055125b365271f4b08faf25124ce2066fad91d`. Corrected the thread record as well.
- 2026-08-19 05:08 UTC - T6 PR #32 shepherd pass found CI green on head `ee055125b365271f4b08faf25124ce2066fad91d` (4 runs / 0 reruns across PR heads), all 4 prior Codex blocking findings fixed in 3 pushes, and all 4 review threads resolved. The PR-body +1 is not attributable on this multi-head PR, so posted the single head-marked Codex review nudge and left the PR waiting for a current-head verdict. Claude Code Review is not configured because its workflow is absent at both the PR head and default branch. Verification: inspected paginated reviews/comments/reactions, complete GraphQL review threads, CI runs, labels, and mergeability; no production tests rerun because this pass changed only factory bookkeeping.
- 2026-08-19 05:12 UTC - T6 PR #32 review tally: Codex delivered 4 rounds, with 4 verifier-classified blocking findings fixed in 3 pushes (0 minor / 0 refuted; 3 fix pushes); its current-head `ee055125b365271f4b08faf25124ce2066fad91d` verdict is clean. Claude Code Review is not configured for this PR because `.github/workflows/claude-code-review.yml` is absent from both the PR head and default branch, despite its legacy remote workflow registration. CI: 4 runs / 0 reruns, green on the current head. All review threads are resolved and there is no hold label. Verification: inspected paginated CI runs, reviews/comments/reactions, complete GraphQL review threads, workflow registration, labels, and mergeability.
- 2026-08-19 05:13 UTC - T6 merged as PR #32 (`f4880a3`). Post-merge bookkeeping marked the task complete, retained its durable PR link, and cleared tracked task/branch/PR state. External review: Codex 4 rounds, 4 blocking fixed / 0 minor parked / 0 refuted, 3 fix pushes; Claude Code Review was not configured on this PR head; CI 4 runs / 0 reruns. Verification: `factory-git merge 32 ee055125b365271f4b08faf25124ce2066fad91d` completed and recorded PR metrics.
- 2026-08-19 05:20 UTC - T8 shipped as PR #33. Existing dashboard code already passes snapshot staleness to the details table on successful and failed refreshes, retains numeric utilization, and replaces live pace judgment with a neutral uncolored `Stale` label; added focused served-JavaScript regression coverage for every acceptance path. Verification: `uv run pytest` (501 passed), `uv run ruff check .` (passed), and `uv run ruff format --check .` (36 files already formatted). Panel: 1 round; findings 0 blocking / 0 minor / 0 invalid; 0 fixed; shipped trivial (planned trivial).
- 2026-08-19 05:22 UTC - T8 PR #33 review tally: Codex delivered 1 clean round on head `090569149570878731ecc207428c9b6bf26741bd` (0 blocking / 0 minor / 0 refuted; 0 fix pushes), verified by its attributable single-head PR-body +1 reaction. Claude Code Review is not configured because its workflow is absent from the PR head and default branch. CI: 1 run / 0 reruns, green on the current head. No review threads or hold label, and the branch is mergeable and current with `main`. Verification: inspected CI, reviews/comments/reactions, force-push events, complete review threads, labels, and mergeability.
- 2026-08-19 05:23 UTC - T8 merged as PR #33 (`7ee56b7`). Post-merge bookkeeping marked the task complete, retained its durable PR link, and cleared tracked task/branch/PR state. External review: Codex 1 clean round, 0 blocking / 0 minor / 0 refuted, 0 fix pushes; Claude Code Review was not configured; CI 1 run / 0 reruns. Verification: `factory-git merge 33 090569149570878731ecc207428c9b6bf26741bd` completed and recorded PR metrics.
- 2026-08-19 05:23 UTC - T8 bookkeeping publish was refused by the protected default-branch ruleset as expected; the committed state remains local and will ride the next task PR. Verification: `factory-git push-bookkeeping` reported that remote `main` did not move; no retry attempted per protocol.
- 2026-08-19 05:32 UTC - T9 shipped as PR #34. `refreshHistory()` now applies same-window outcomes newer than the last rendered outcome even when a later request is in flight, drops genuinely older outcomes, and retains explicit range-identity guards on success and failure; added served-JavaScript regression coverage for the complete guard structure. Verification: `uv run pytest` (502 passed), `uv run ruff check .` (passed), `uv run ruff format --check .` (36 files already formatted), and `node --check src/burnrate/static/app.js` (passed). Panel: 1 round; findings 0 blocking / 0 minor / 0 invalid; 0 fixed; shipped trivial (planned trivial).
- 2026-08-19 05:36 UTC - T9 PR #34 review tally: Codex delivered 1 clean round on head `65264c8ea2eb363dc29de5110255e75d6f41d900` (0 blocking / 0 minor / 0 refuted; 0 fix pushes), verified by its attributable single-head PR-body +1 reaction. Claude Code Review is not configured because `.github/workflows/claude-code-review.yml` is absent from the PR head and default branch, and no head workflow run exists. CI: 1 run / 0 reruns, green on the current head. No review threads or hold label; branch is mergeable and current with `main`. Verification: inspected CI, reviews/comments/reactions, force-push events, complete review threads, labels, workflow registration, and mergeability.
